- trigger: fd-ref
  scope: jade
  text: |
        fd-ref(
            fd-ref-path="${1:$ctrl.$path}"
            fd-ref-query="${2:$ctrl.$query}"
            fd-ref-array="${3:false}"
            then="${4:$data}"
            catch="${5:$error}")
            before
            then
            catch
            