- trigger: fd-upload
  scope: jade
  text: |
        fd-upload(
            fd-upload-file="${1:$file}"
            fd-upload-filename="${2}"
            before="${3:$data}"
            then="${4:$data}"
            catch="${5:$error}")
            before
            then
            catch
            