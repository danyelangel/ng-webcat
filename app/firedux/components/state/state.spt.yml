- trigger: fd-state
  scope: jade
  text: |
        fd-state(
            fd-state-path="${1:$ctrl.$path}"
            fd-state-query="${2:$ctrl.$query}"
            fd-state-array="${3:false}"
            then="${4:$data}"
            catch="${5:$error}")
            before
            then
            catch
            