md-menu-item
  md-button(
  ng-click="$ctrl.onClick({$event:$event})")
    div(layout="row" flex)
      p(
        ng-transclude=""
        flex)
      md-icon(
      md-menu-align-target
      style="margin: auto 3px auto 0;")
          |{{$ctrl.wcIcon}}